[project]
name = "greenline-lwse-v-home-assistant"
version = "0.3.0"
description = "Greenline LWSE-V custom integration for Home Assistant"
readme = "README.md"
requires-python = ">=3.14.2"
license = "Apache-2.0"
dependencies = []

[dependency-groups]
quality = [
  "mypy==2.3.0",
  "pre-commit==4.6.2",
]
test = [
  "pytest-homeassistant-custom-component==0.13.355",
]

[tool.uv]
package = false
prerelease = "allow"
